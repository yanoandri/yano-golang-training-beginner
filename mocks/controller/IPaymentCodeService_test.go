// Code generated by mockery 2.7.5. DO NOT EDIT.

package mocks

import (
	"errors"
	"fmt"
	"reflect"
	"testing"
	"time"

	mock "github.com/stretchr/testify/mock"
	model "github.com/yanoandri/yano-golang-training-beginner/model"
)

func TestIPaymentCodeService_CreatePaymentCode(t *testing.T) {
	dateExpired := fmt.Sprintf("%s", time.Now().AddDate(50, 0, 0).UTC())
	type fields struct {
		Mock mock.Mock
	}
	type args struct {
		payment model.PaymentCodes
	}
	tests := []struct {
		name    string
		fields  fields
		args    args
		want    model.PaymentCodes
		wantErr bool
	}{
		// TODO: Add test cases.
		{
			name:   "Success create payment code",
			fields: fields{},
			args: args{
				model.PaymentCodes{
					Name:           "lechsa",
					PaymentCode:    "XXX-123",
					Status:         model.Active,
					ExpirationDate: dateExpired,
				},
			},
			want: model.PaymentCodes{
				Name:           "lechsa",
				PaymentCode:    "XXX-123",
				Status:         model.Active,
				ExpirationDate: dateExpired,
			},
			wantErr: false,
		},
		{
			name:   "Failed create payment code",
			fields: fields{},
			args: args{
				model.PaymentCodes{
					Name:           "lechsa",
					PaymentCode:    "XXX-123",
					Status:         model.Active,
					ExpirationDate: dateExpired,
				},
			},
			want:    model.PaymentCodes{},
			wantErr: true,
		},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			_m := &IPaymentCodeService{
				Mock: tt.fields.Mock,
			}
			if !tt.wantErr {
				_m.On("CreatePaymentCode", tt.args.payment).Return(tt.want, nil)
			} else {
				_m.On("CreatePaymentCode", tt.args.payment).Return(tt.want, errors.New("errors"))
			}

			got, err := _m.CreatePaymentCode(tt.args.payment)
			if (err != nil) != tt.wantErr {
				t.Errorf("IPaymentCodeService.CreatePaymentCode() error = %v, wantErr %v", err, tt.wantErr)
				return
			}
			if !reflect.DeepEqual(got, tt.want) {
				t.Errorf("IPaymentCodeService.CreatePaymentCode() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestIPaymentCodeService_Get(t *testing.T) {
	type fields struct {
		Mock mock.Mock
	}
	type args struct {
		id string
	}
	tests := []struct {
		name    string
		fields  fields
		args    args
		want    model.PaymentCodes
		wantErr bool
	}{
		// TODO: Add test cases.
		{
			name:   "Success get payment code by id",
			fields: fields{},
			args: args{
				id: "0000-0000-0000-000",
			},
			want: model.PaymentCodes{
				Name:           "lechsa",
				PaymentCode:    "XXX-123",
				Status:         model.Active,
				ExpirationDate: fmt.Sprintf("%s", time.Now().AddDate(50, 0, 0).UTC()),
			},
			wantErr: false,
		},
		{
			name:   "Failed get payment code by id",
			fields: fields{},
			args: args{
				id: "0000-0000-0000-000",
			},
			want:    model.PaymentCodes{},
			wantErr: true,
		},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			_m := &IPaymentCodeService{
				Mock: tt.fields.Mock,
			}

			if !tt.wantErr {
				_m.On("Get", tt.args.id).Return(tt.want, nil)
			} else {
				_m.On("Get", tt.args.id).Return(tt.want, errors.New("errors"))
			}

			got, err := _m.Get(tt.args.id)
			if (err != nil) != tt.wantErr {
				t.Errorf("IPaymentCodeService.Get() error = %v, wantErr %v", err, tt.wantErr)
				return
			}
			if !reflect.DeepEqual(got, tt.want) {
				t.Errorf("IPaymentCodeService.Get() = %v, want %v", got, tt.want)
			}
		})
	}
}
